import setuptools

setuptools.setup(
        name="thinkplot",
        version="0.1",
        packages=setuptools.find_packages(),
        install_requires=[
            'pandas',
            'numpy',
            'matplotlib'
            ]
        )
